const(<pos>): {
	let(<x>): 0
	let(<y>): 0
}
fn(<person>): {
	let(<name>): p(0, "string")
	return(self)
}

const(<abc>): {
	include(pos)
	event("explosion")

	fn(<explode>): {
		x(rand(-2, 3))
		y(rand(-2, 3))
		emit("explosion")
	}
}

const(<player>): {
	include(pos)
	include(person("Player"))
}

abc.on("explosion"): {
	print(add(str(abc.x), ", ", str(abc.y)))
	print(player.name)
}
abc.explode()