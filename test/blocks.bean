const(<block>): {
	let(<value>): 42
	
	fn(<function>): {
		print("Hello from inside a function! param 0:", p(0))
		yield("hi")
	}
}

{
	let(<x>): 2
	print("Getting value:", block.value)
	block.function("test"): {
		// __debug
		print("from a yield", x)
	}
}