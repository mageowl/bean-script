<!web>
web.useElementAsConsole("fscript-logs");

def(<lessThan>) -> {
	def(<diff>) -> sub(param(0), param(1));
	def(<less>) -> false;

	unless(is(sub(abs(diff), diff)) -> 0) -> {
		set(<less>) -> true;
	}

	return(less);
}


if(lessThan(10, 30)) -> {
	print("10 is less than 30.");
};

defI(<mem>) -> <value>;

def(<func>) -> {
	def(mem) -> "Wow!"
}

func;
print(value);