<!web>
web.useElementAsConsole("fscript-logs");

let(<console>) -> web.getElement("#fscript-logs");
let(<can>) -> web.createElement("span");
can.text("OP POND");
can.setAttr("style", "font-weight:bold;");
console.add(can);

web.on("keydown") -> {
	print(event.key);
};
